---
title: Dot.docs — Platform Wiki
version: 0.8.0
status: draft
owners: [Docs Platform Lead]
platform-id: dot-docs
last-review: 2026-09-10
---

# Dot.docs

Purpose: this is Dot.docs' own knowledge home — owned and maintained by the Dot.docs team. It describes what this platform actually is, as implemented, and how it connects to the wider Dot Ecosystem. Dot.Brain never edits this file; it only reads what we choose to publish.

> **Related:** [Dot.Brain's ingested view of this platform](https://github.com/sakhilebhayi/Dot.Brain/blob/main/platforms/dot-docs.md) — this file does not exist yet at the time of writing; a separate ingestion process creates it once Dot.docs publishes its first Knowledge Pack.

---

## 1. What Dot.docs Is

Dot.docs is a real-time collaborative document/wiki platform for the InfoDot ecosystem — closer to Notion or Confluence than to a static docs site. Teams and individuals create rich-text documents, edit them together live (presence, active-user avatars, broadcast content updates), track full version history with side-by-side diffs, comment inline with threaded replies and @mentions, and share documents publicly via password- and expiry-protected links. It layers an AI writing assistant (grammar check, summarize, continue writing, tone rewrite, translate, outline) and a slash-command palette (`Ctrl+K`) on top, plus export/import to PDF, Word, HTML, and Markdown.

**Status:** this is a substantially built application — real models, migrations, ~13 Livewire components, a working `EcosystemAuthController`, broadcast events, and a genuine (not faked) OpenAI integration behind `AiService` — not a scaffold. It was previously untouched by the ecosystem-wide engineering effort; this pass is its first. Treat §8 (Roadmap) as what's genuinely unbuilt (Phase 10 in the repo's own `task-list.md` — Pest tests, Dusk browser tests, Telescope/Pulse, error tracking — was left unchecked and still is), and everything else in this document as grounded in the code that exists today.

## 2. Architecture

| Layer | Technology | Notes |
|---|---|---|
| Framework | Laravel 13, PHP 8.3+ (composer.json `"php": "^8.3"`) | Jetstream 5.5 (Livewire stack, Teams), Fortify for auth actions |
| Editor | TipTap 3 (`@tiptap/core` + a custom extension set: `blockId`, `callout`, `columns`, `crossRef`, `docAttrs`, `figure`, `headingNumbered`, `image`, `pageBreak`, `sectionBreak`, `textAlign`, `toc`, `variable`) on `resources/js/editor/**` | `content_json` (a validated ProseMirror-shaped tree, see `App\Documents\Schema\DocumentSchema`) is the canonical document format as of this Phase 1 pass; `content` (rendered HTML) is now a derived/legacy mirror, not the source of truth. `dot:documents:migrate-json` backfills `content_json` for any row still missing it. |
| UI | Livewire 3, Alpine.js 3, Tailwind CSS via Vite (the CDN Tailwind/Alpine build used before this pass is gone) | "Two Inks on a Desk" shell redesign (Task 9): a mono status line, ledger-style panels, desk-seam hairlines instead of cards, full night/day themes — see `docs/design/2026-09-09-shell-design-note.md` |
| Document Styles | `App\Styles\StyleEngine` + `App\Styles\CssBuilder` + `App\Styles\TokenGuard`, fourteen system rows seeded by `DocumentStyleSeeder` (`document_styles` table) | Corporate, Executive, Academic, Legal, Financial, Technical, Government, Mining, Engineering, Marketing, Proposal, Report, Minimal, Creative — each a token set (fonts, sizes, numbering, table/caption rules, page layout) compiled to CSS for the editor/print surfaces and to DOCX styles for export; a team can override a system style by row, resolved by `DocumentStyle::resolve()` (team row wins over the system default). `TokenGuard` sanitises every token before it reaches CSS or DOCX. |
| Print / PDF | `App\Print\PrintRenderer` + `App\Print\PageSetup` + `barryvdh/laravel-dompdf` | Server-side, page-accurate PDF: `PageSetup::fromDocument()` merges the document's own `page_setup` override over its resolved style's `page` tokens; header/footer bands support `{{ title }}`/`{{ date }}`/every `variables` key server-side and `{{ page }}`/`{{ pages }}` via dompdf's own canvas page-numbering |
| Import / Export | `App\Documents\Import\{DocxImporter,MarkdownImporter,PlainTextImporter,HtmlToJson}`, `App\Documents\Export\{DocxExporter,MarkdownExporter}` (`phpoffice/phpword`) | Structured, schema-aware round-trip (headings, lists, tables, marks) — replaced the earlier `league/html-to-markdown`-only path; that dependency was dropped (`780822b`) |
| Database | PostgreSQL (dev/prod), SQLite in-memory for the test suite (`phpunit.xml` forces `DB_CONNECTION=sqlite`) | `documents.search_vector` is a Postgres `GENERATED ALWAYS AS ... STORED` `tsvector` column over `title`+`search_text` (migration `2026_09_07_000001`); `App\Search\DocumentSearch` degrades to `LIKE` on SQLite. Ecosystem-shared `DB_DATABASE=infodot` remains the deployed default; local dev/verification typically points at a dedicated `dot_docs_verify` database instead (see §9's successor note in the Change Log). |
| Auth | Laravel Sanctum + Jetstream Teams + a custom `EcosystemAuthController` | SSO handoff from the InfoDot hub at `/auth/ecosystem` |
| Realtime | Laravel Reverb | Presence channels per document (`document.{id}`), used for live cursors/avatars, content broadcast, and comment broadcast — genuinely wired, not just configured; unchanged by this Phase 1 pass |
| AI | `prism-php/prism` via `App\Ai\Client` (+ `App\Ai\Usage`, `App\Ai\AiResult`) | Replaced the earlier direct `openai-php/laravel` integration. `config('ai.provider')` defaults to and is test-forced to `mock` (`AI_PROVIDER=mock`, no network call, deterministic output) — the safe default. `AI_PROVIDER=live` routes through Prism: Anthropic is the primary provider (`claude-sonnet-5`/`claude-opus-5`/`claude-haiku-4-5-20251001` by role), with a `config('ai.failover')` chain (a cheaper Anthropic model, then OpenAI's `gpt-4o`) walked in order on a provider exception. Every call — mock included — writes one `ai_model_usage` row via `App\Ai\Usage`. Still rate-limited (`config('ai.rate_limit_per_hour')`, default 20/user/hour). |
| Audit | `App\Audit\AuditLogger` (service) writing `App\Models\AuditLog` (model) rows | Append-only: the model's `booted()` throws `LogicException` from `updating`/`deleting` hooks, so a tampering attempt fails loudly rather than silently. Records actor/action/subject/context/ip/user-agent for saves, exports, shares, and AI calls. |
| Cache / Session / Queue | Database driver | No Redis, Horizon, Scout, or Meilisearch dependency exists in `composer.json` despite `task-list.md` listing them as planned |
| Storage | Local disk (Flysystem) | No S3/AWS credentials or disk config actually wired in beyond the default `.env` placeholders |

Team/user scoping runs through Jetstream's `Team` model, but Dot.docs is **not** exclusively team-scoped — a document can be owned by an individual user (`owner_id`), optionally attached to a team (`team_id`), and independently shared to specific collaborators (`document_collaborators`), published to the public internet by slug (`/d/{slug}`, optional password/expiry, view counter), or reached by its legacy `/shared/{uuid}` link. `DocumentPolicy` is the single source of truth for all these access paths.

## 3. Domain Entities (as implemented)

Source: `database/migrations/*`, `app/Models/`.

| Model | Table | Purpose |
|---|---|---|
| `Document` | `documents` | Core entity — UUID, title, structured `content_json` (canonical) + legacy `content` HTML mirror, `search_text`/generated `search_vector`, owner, optional team, folder, style key, brand kit, per-document `page_setup`/`variables` override, version counter, review state, publish slug + view counter, public-share settings (password hash, expiry); soft-deletable |
| `DocumentVersion` | `document_versions` | Immutable content snapshot (HTML **and** `content_json`), auto-created by `DocumentObserver`/`DocumentStore` on every content change (collapsing rapid autosaves into one version inside a time window); powers the diff/restore UI |
| `DocumentStyle` | `document_styles` | One of the fourteen system styles, or a team's override of one (`key`+`team_id` unique; a Postgres partial unique index enforces one system row per key). `tokens` (jsonb) drives fonts/sizes/numbering/table/caption/page rules; resolved per document by `DocumentStyle::resolve()` / `StyleEngine` |
| `BrandKit` | `brand_kits` | A team's logo, fonts, colours, letterhead/footer templates, disclaimer, contact block, and default style key — applied to a document's print/export surfaces via `brand_kit_id` |
| `DocumentTemplate` | `document_templates` | Reusable starting content — now `content_json` + `style_key` + `page_setup`, not just HTML; global, team-owned, or personal (`created_by` nullable, so a system/global template needs no owning user) |
| `DocumentSuggestion` | `document_suggestions` | A pending AI- or human-authored patch (`patch` jsonb) against a specific `block_id`, `status` pending/accepted/rejected, resolved by a user (distinct from the older `AiSuggestion`/`ai_suggestions`, which still exists for the suggestion-mode track-changes feature) |
| `AuditLog` | `audit_logs` | Append-only — no `updated_at`, model throws on update/delete. `actor_type`/`actor_id`, `action`, polymorphic `subject_type`/`subject_id`, `context` (jsonb), `ip`, `user_agent` |
| `AiModelUsage` | `ai_model_usage` (singular table name — the model overrides Eloquent's pluraliser) | One row per AI call (mock calls included): provider, model, operation, input/output/cache-read tokens, `cost_usd`, `latency_ms`, `fallback_used` |
| `DocumentCollaborator` | `document_collaborators` | Per-user role grant on a document (`viewer`/`editor`/`admin`) independent of team membership |
| `Comment` | `comments` | Threaded (`parent_id`), resolvable, tied to a text selection (`selection_text`) and now also a `block_id` on the structured document |
| `AiSuggestion` | `ai_suggestions` | A pending AI-proposed edit in "suggestion mode," accepted/rejected by a collaborator (pre-existing; unchanged by this pass) |
| `DocumentSlashCommand` | `document_slash_commands` | User- or team-defined `/command` shortcut that expands to a prompt template for the AI assistant |
| `DocumentWebhook` | `document_webhooks` | Outbound webhook definition per document, fired on `on_save` / `on_export`; the target URL is checked by `App\Support\SsrfGuard` (including on redirect) before `WebhookService` fires it |
| `Folder`, `Tag` | `folders`, `tags`, `document_tag` | Organisational structure for the document index (pre-existing; unchanged by this pass) |
| `Team`, `TeamInvitation`, `Membership`, `User` | Jetstream defaults | Standard Jetstream Teams tables, extended with `bio`/`preferences` on `users` |

## 4. SSO Contract Verification

`app/Http/Controllers/Auth/EcosystemAuthController.php` matches the ecosystem-standard pattern used by every other platform:

1. `PersonalAccessToken::findToken($request->query('token'))`
2. `abort_if` on: token missing, token lacks the `ecosystem:read` ability, or token is past `expires_at`
3. Resolves the tokenable `User`, **deletes the token** (one-time use)
4. `Auth::login($user)`
5. Redirects to this app's own `dashboard` route

This is correct and consistent with the contract described in [Dot.Billing's wiki.md](../Dot.Billing/wiki.md) §2.

**Database finding (fixed this pass):** `.env.example` had `DB_DATABASE` commented out as `# DB_DATABASE=laravel`, meaning any environment built from the example file — before this fix — would fall back to `config/database.php`'s hardcoded default of `'laravel'`, a database that does not exist in the shared InfoDot PostgreSQL instance. Every other platform's `.env.example` (Dot.Billing, Dot.Tasks, Dot.Projects checked directly) sets `DB_DATABASE=infodot` uncommented. This has been corrected to `DB_DATABASE=infodot` in this pass — see Change Log. This was not caught by CI or any prior review because this environment cannot run `php artisan migrate` or connect to Postgres at all (see §9); it was found by reading the config, not by running it.

**Recent commit check:** the most recent commit before this pass, `afac9fe "fix: update routes/web.php imports and dashboard query"`, replaced a stub `return view('dashboard')` with real KPI queries (`myDocs`, `sharedDocs`, `publicDocs`, `aiSuggestions`, `recentDocs`, `recentShared`) and passes them to `dashboard.blade.php`, which consumes every one of those variables. Read side-by-side with the view, the fix is complete and internally consistent — nothing was left half-wired.

## 5. Events Emitted

Real, broadcast-over-Reverb domain events exist today (not aspirational):

| Event | Channel | Trigger | Broadcast payload |
|---|---|---|---|
| `DocumentUpdated` | `document.{id}` (presence) | Live content edit propagated to other active collaborators | document id, content, version, editor identity |
| `UserJoinedDocument` / `UserLeftDocument` | `document.{id}` (presence) | Collaborator opens/leaves the editor | user identity / user id |
| `CommentPosted` | `document.{id}` (presence) | New comment or reply | full comment payload including author |

These are genuine `ShouldBroadcast` events wired to real Livewire components (`Editor`, `CommentThread`) and `PresenceService` — not stubs. There is no outbound Dot.Brain-facing Knowledge Pack publisher yet; these events are internal to the collaborative-editing feature only, not (yet) republished as ecosystem-facing domain events.

## 6. Security & Technical-Debt Scan (this pass)

Scope: by-ID record lookups reachable from Livewire public methods, checked against the same IDOR pattern found repeatedly across the ecosystem this session (an unscoped `Model::find($id)` where the ID is a user-controllable Livewire method argument).

**`DocumentPolicy` and most Livewire components are solid** — every document-scoped component (`Editor`, `DocumentSettings`, `ShareManager`, `WebhookManager`, `CommentThread`, `AiAssistant`, `AiChat`, `SaveAsTemplate`, `VersionHistory`) calls `$this->authorize(...)` against `DocumentPolicy` before mutating state, and nested lookups (comments, webhooks, versions used for `restore()`) are correctly scoped with `->where('document_id', $this->document->id)`.

**Two real gaps found and fixed:**

1. **`VersionHistory::runDiff()` and the `previewVersion` computed value** (`app/Livewire/Documents/VersionHistory.php`) called `DocumentVersion::find($id)` on Livewire-supplied version IDs (`compareIds`, `previewId`) with no scoping to the authorized document. A user with legitimate view access to Document A could pass version IDs belonging to Document B — one they have no access to — via `toggleCompare()`/`preview()`, and the diff/preview panel would render Document B's content snapshot. Fixed by scoping both lookups to `->where('document_id', $this->document->id)`.
2. **`TemplateGallery::useTemplate()`** (`app/Livewire/Documents/TemplateGallery.php`) called `DocumentTemplate::findOrFail($templateId)` with no visibility check, while the component's own `templates()` listing correctly restricts to global, own-team, or self-authored templates. Any authenticated user could pass an arbitrary `templateId` and copy another team's private template content into a new document. Fixed by applying the same visibility scope used in `templates()` before the lookup.

Both fixes are narrow (query-scoping only, no schema or authorization-model changes) and match the existing `DocumentPolicy`/`templates()` conventions rather than introducing a new pattern.

**Not fixed, flagged for a dedicated pass:** `Document::findByUuidCached()` and `cachedContent()` cache by UUID/document id but never by user — fine for public documents, but worth a second look if private-document caching is ever added on this code path (currently it is not; private documents always read `$this->content` directly, bypassing the cache — see `Document::cachedContent()`). No action taken; flagging only, per the bounded-pass rule in [02-Engineering-Loop.md](../Dot.Brain/os/02-Engineering-Loop.md) §5.

## 7. Branding

`dot_doc.png` (repo root) was already wired into `application-logo.blade.php`, `application-mark.blade.php`, and `authentication-card-logo.blade.php`, and referenced directly as `/dot_doc.png` for the favicon `<link>` in `welcome.blade.php` and `layouts/guest.blade.php`. `layouts/app.blade.php` (the main authenticated app shell) had no favicon link at all.

This pass added the standard ecosystem favicon set generated via `sips` (`apple-touch-icon.png` 180px, `favicon-32x32.png`, `favicon-16x16.png`), copied the source logo to `public/images/logo.png`, and wired the three-tag favicon block (`32x32`, `16x16`, `apple-touch-icon`, all via `asset()`) into all three head-bearing layouts (`layouts/app.blade.php`, `layouts/guest.blade.php`, `welcome.blade.php`) — matching the exact pattern already used in Dot.Billing's layouts.

## 8. Connecting to Dot.Brain

Dot.docs is not yet registered as publishing Knowledge Packs — there is no outbound publisher, no aggregation-floor configuration, and no DKP manifest in this repo. Dot.Brain's ingested view of this platform is expected to live at [`platforms/dot-docs.md`](https://github.com/sakhilebhayi/Dot.Brain/blob/main/platforms/dot-docs.md); that file does not exist yet as of this writing (it is created by a separate ingestion process, not by this repo).

If/when publishing begins, the natural payload shape follows the same pattern as other platforms:

| Payload type | Would contain |
|---|---|
| `observation` | Aggregated document/collaboration metrics (documents created, active-collaboration sessions, AI-assist usage) — never document content |
| `insight` | Patterns in AI-assist acceptance rates, template usage, collaboration density |
| `outcome` | Verification of any Dot.Brain recommendation (e.g., "suggest templates to teams with low document creation") |
| `incident` | Broadcast/Reverb outages, AI provider failures |

Given that `documents` and `comments` can contain arbitrary user content (including on shared/public documents with real names and text), any aggregation published outward must never include raw content or comment text — only counts and metadata. No aggregation or publishing code exists yet, so this is a design requirement to build in before publishing begins, not an enforced constraint today.

## 9. Environment Constraints on This Pass

This pass was hand-authored and hand-reviewed only. **No PHP, Composer, PostgreSQL, or Docker were available** in the working environment — nothing in this document was verified by running `php artisan`, executing a migration, or hitting a real Postgres instance. All findings (the `DB_DATABASE` misconfiguration, the two IDOR gaps, the dashboard-query commit review) were made by reading the actual source files side-by-side, not by executing them. CI or a genuine PHP/Postgres dev environment remains a mandatory gate before any of this reaches production, per [02-Engineering-Loop.md](../Dot.Brain/os/02-Engineering-Loop.md) §2.

## 10. Roadmap / Open Questions

- [ ] Feature/Pest test suite — `task-list.md` Phase 10.1 is entirely unchecked; no `tests/Feature` coverage for document CRUD, authorization, or the two IDOR fixes made this pass exists yet
- [ ] Browser (Dusk) tests for collaborative editing — unchecked in `task-list.md`
- [ ] Production deployment / queue worker / monitoring (Telescope, Pulse, Sentry) — Phase 10.2–10.3, unchecked
- [ ] No Knowledge Pack publisher or DKP manifest exists — prerequisite for any Dot.Brain integration beyond this static wiki
- [ ] `task-list.md` still describes OpenAI/Gemini as an either/or choice and Redis/Horizon/Scout/Meilisearch as part of the stack; only OpenAI and a database-backed cache/queue actually shipped — `task-list.md` itself was not corrected this pass (it is a historical planning artifact, left as-is; this wiki is the accurate source going forward)
- [ ] Whether `Document::cachedContent()`'s public-only caching should be extended (carefully, with per-user keys) to private documents is an open design question, not a bug — flagged in §6, not fixed

## Change Log

| Version | Date | Author | Change |
|---|---|---|---|
| 0.8.0 | 2026-09-10 | Dot.Doc Phase 1 pass | **Phase 1 foundation, wrap-up (Task 13).** The largest single-branch change to this platform since 0.1.0: `content_json` (a validated ProseMirror JSON tree, `App\Documents\Schema\DocumentSchema`) replaces HTML as the canonical `Document`/`DocumentVersion`/`DocumentTemplate` format, with `App\Console\Commands\MigrateDocumentsToJson` backfilling every pre-existing row (run this pass against `dot_docs_verify`: 1 document backfilled, 0 templates needed it; verified after via a read-only query that every non-trashed `Document` and `DocumentTemplate` has a non-null `content_json`). Replaced the CDN Tailwind/Alpine editor bundle with TipTap 3 on a custom extension set (`resources/js/editor/**`: `blockId`, `callout`, `columns`, `crossRef`, `docAttrs`, `figure`, `headingNumbered`, `image`, `pageBreak`, `sectionBreak`, `textAlign`, `toc`, `variable`) plus a command palette and `/` slash menu. Added outline numbering, a live table of contents, and cross-references (`App\Documents\Outline\*`). Added the page model — per-document `page_setup` over a style's own page tokens (`App\Print\PageSetup`), a print-preview toggle in the editor, and a genuine server-side PDF surface (`App\Print\PrintRenderer` + dompdf) with templated headers/footers. Shipped the Document Styles engine (`App\Styles\StyleEngine`/`CssBuilder`/`TokenGuard`) and its fourteen system styles (Corporate, Executive, Academic, Legal, Financial, Technical, Government, Mining, Engineering, Marketing, Proposal, Report, Minimal, Creative — `DocumentStyleSeeder`), plus a per-team `BrandKit` model. Replaced HTML-string import/export with structured, schema-aware DOCX and Markdown round-trips (`App\Documents\Import\*`, `App\Documents\Export\*`); dropped the now-unused `league/html-to-markdown` dependency. Added a template gallery with global/team/personal visibility and a South African starter set (monthly production report, safety incident report, board memorandum — `StarterTemplateSeeder`), and slug-based publishing (`/d/{slug}`, optional password + expiry, view counters) alongside the existing named-collaborator and legacy `/shared/{uuid}` paths. Added an append-only `AuditLog` (immutable at the model level — `updating`/`deleting` throw), PostgreSQL full-text search over a generated `search_vector` column (`App\Search\DocumentSearch`, `LIKE` fallback on SQLite), and replaced the direct OpenAI integration with `prism-php/prism` (`App\Ai\Client`) — `mock` provider by default and forced in tests (no network call, no key required), Anthropic as the live primary, OpenAI as the failover leg, every call (mock included) accounted in the new `ai_model_usage` table. `WebhookService::fire()` now runs every target URL (including redirects) through `App\Support\SsrfGuard` before firing. Gave the whole authenticated shell a design pass — "Two Inks on a Desk" (`docs/design/2026-09-09-shell-design-note.md`): a mono status line, ledger-style panels, desk-seam hairlines, and full night/day themes, verified against WCAG contrast and keyboard/focus rules. §2 and §3 above are rewritten against the actual current migrations and models (not carried over from 0.7.0) to reflect all of this. **PHPStan:** `vendor/bin/phpstan analyse --memory-limit=1G` (level 5, Larastan) reports 72 pre-existing-pattern findings across the app (55 of them in files this pass touched); traced each to one of three systemic, already-present gaps — `Auth::user()`/team-relation calls resolving to the generic `Illuminate\Database\Eloquent\Model` because Jetstream's `User`/`Team` relations carry no Larastan-recognizable generics (present on `Actions/Jetstream/*` and other untouched files too), Livewire's `#[Computed]` properties being invisible to PHPStan without a Livewire-specific extension (none installed), and stale PHPDoc on `phpoffice/phpword`/DOM APIs whose real runtime behaviour is looser than documented (spot-checked `Field::__construct()`'s undocumented 5th `$fontStyle` parameter and `Settings::setDefaultFontSize()`'s actual `is_int()||is_float()` guard — both fixes-that-aren't; the flagged calls work correctly today) — none is a bug this pass introduced, none fixed, per the "fix new findings only, note what's deliberately left" instruction. **Pint:** clean, no changes. **Tests:** `php artisan test --compact` — 273 passed, 4 skipped, 0 failed (1808 assertions). **Related:** [Phase 1 implementation plan](docs/superpowers/plans/2026-09-07-dot-doc-phase-1-foundation.md), [Dot.Doc platform design spec](docs/superpowers/specs/2026-09-07-dot-doc-platform-design.md). |
| 0.7.0 | 2026-08-07 | Platform-loop pass | Ecosystem-wide standardization pass: branded transactional email theme, POPIA-aligned legal pages, and hero-styled auth pages, matching the pattern piloted on InfoDot and rolled out to all 25 other platforms in the ecosystem — **this is the final platform in that rollout.** **Legal pages:** enabled `Features::termsAndPrivacyPolicy()` in `config/jetstream.php` (previously commented out; `policy.blade.php`/`terms.blade.php` already existed, correctly styled per 0.6.0, but were unreachable). Wrote `resources/markdown/{policy,terms,cookies}.md` from Jetstream's stock 3-line placeholders, naming **BluePin Inc** as the responsible party, aligned with South Africa's POPIA, grounded in this platform's real multi-path access model (§2/§6: personal / team-attached / named-collaborator / public-with-password-and-expiry, `DocumentPolicy` as the single source of truth) — no operator/third-party split needed, since sharing (even public sharing) stays within Dot.docs' own product surface. Explicitly discloses that the AI writing assistant sends the relevant document text to OpenAI (§2's real, non-faked `AiService` integration, rate-limited 20 req/user/hour) only when actively used, and that public share links can be password- and expiry-protected. Added a hand-wired `/cookies` route reading the new `cookies.md`. **Known pre-existing gap, not fixed this pass:** `.env`'s `APP_NAME` is still the stock `Laravel` value (confirmed via both `.env` and `.env.example`) — same class of issue already documented on Dot.Engage/Dot.Forms. Left untouched since `.env` is off-limits; the Cookie Policy honestly names the real current cookie (`laravel-session`, not an invented `dot-docs-session`), and the mail theme's header/footer branding is hardcoded to "Dot.docs" directly in `message.blade.php` rather than trusting `config('app.name')`, so only Fortify's own notification body text ("Regards, Laravel") is affected by the stale name — confirmed live in the mail preview, flagged not fixed, consistent with precedent. Footer of `welcome.blade.php` got Privacy/Cookies/Terms links. **Auth hero photo:** `components/authentication-card.blade.php` had a flat `--paper` background with no photo, despite the welcome page having a real hero photo ("type type type," hands typing on a laptop, by Christin Hume, `photo-1515378960530-7c0da6231fb1`) with a dark-ink scrim. Since this platform's overall theme (confirmed via `guest.blade.php` and the welcome page's own structure) is light, applied the ecosystem's light-paper scrim variant to the auth card instead of a literal copy of the hero's own dark treatment — same reasoning applied on every other light-themed platform in this rollout (Files/Billing/HR/Sheet/Tutor). Verified across `/login` at desktop (1600px) and mobile (375px): photo and scrim render correctly, white card fully legible, dual-tone gold/blue logo legible against the photo, no overflow. **Branded transactional email theme:** published Laravel's markdown mail vendor views for the first time in this repo and rewrote both `message.blade.php` (real `<img>` logo, hardcoded "Dot.docs"/"Real-time collaborative documents." footer) and `themes/default.css`, using the platform's own pre-existing `--gold-deep` token directly for links since it's already a correctly-deep, AA-contrast-safe gold — no new color needed, unlike most sibling platforms. Verified via a temporary `/_mail-preview` route rendering `ResetPassword::toMail()` — dark header with logo, gold button with correct dark-ink text, branded footer, "Regards, Laravel" confirmed as the one pre-existing, documented, out-of-scope artifact — removed before commit. **Verification:** `npm run build` clean; `php artisan config:clear && route:clear && view:clear`; full suite green, 43/43 non-skipped tests, 0 failed, unchanged from pre-pass; browser-verified `/login` and `/privacy-policy` at desktop and mobile widths, zero console errors. |
| 0.6.0 | 2026-08-06 | Platform-loop pass | Redesigned the guest-facing auth page family (login, register, forgot-password, reset-password, confirm-password, two-factor-challenge, verify-email, terms, policy) to match `welcome.blade.php`'s established brand (0.5.0). Unlike most sibling platforms, this was **not** Jetstream's stock scaffold — `layouts/guest.blade.php` carried its own bespoke split-screen SaaS template (a dark `#080f1e` left branding panel with radial glow blobs, a dot-grid background, glassmorphism "glass-card" feature tiles, a gradient-text headline "Your documents, reimagined.", and a `Figtree`-only font stack), none of it grounded in the real `--paper #fbf7ee`/`--ink #1f1b14`/`--gold #f1c62e`/`--blue-deep #1a6bae`/`Fraunces`/`Work Sans`/`IBM Plex Mono` tokens established on the welcome page — the same generic-dark-SaaS-template failure mode the 0.5.0 entry above already found and removed from the welcome page itself, just not yet from the auth pages. Two of that panel's own CSS classes (`.form-input`, `.btn-primary`) were dead code — grepped `resources/views/auth/` and `resources/views/components/` and confirmed zero references; the actual field components use `x-label`/`x-input`/`x-button`, which carried their own separate off-brand `slate`/`sky`/`amber` Tailwind palette. Replaced `layouts/guest.blade.php` wholesale with the same simple single-column pattern used across the rest of the ecosystem (paper background, welcome page's fonts/tokens, no split panel) — the marketing-style left panel duplicated the welcome page's own job and violated this pass's "keep it simple, no over-decoration" constraint for utility pages; the removed panel's copy ("AI Writing Assistant", "Works Offline", etc.) was marketing framing, not something these forms needed to carry. `authentication-card.blade.php` (previously had special mobile-only-logo logic to accommodate the split panel) simplified to the standard logo-above-card pattern; `authentication-card-logo.blade.php` fixed to reference `asset('images/logo.png')` (was a root-relative `/dot_doc.png` string) at `h-14 sm:h-16`, the exact size this platform's own welcome page nav established (not the ecosystem-typical `h-16 sm:h-20` — this platform's logo was deliberately sized smaller there, so that's what was reused here too). Retokenized `label`/`input`/`button`/`checkbox`/`validation-errors` from the ad-hoc `slate-200`/`sky-400`/`#F5C110` gradient/`amber-300` values to the real `--gold`/`--gold-deep`/`--line`/`--ink` tokens. Swept `auth/*.blade.php` (which already had good bespoke per-page headings like "Welcome back" / "Create your account" — kept those, just retokenized their `slate-900`/`slate-500`/`sky-600` colors). Used `--blue-deep` rather than `--gold` for link text on the white card, matching how welcome.blade.php itself only ever uses `--blue-deep` (not `--gold`) for text directly on light/paper surfaces. `terms.blade.php`/`policy.blade.php` (Jetstream's terms feature is commented out; only `policy` has no live route, `terms` was never routed as `terms.show` either) got the same paper/card frame for consistency. No `route()`/`action`/`@csrf`/input `name`/validation-display changes anywhere. Verified: `npm run build` clean; created and migrated a fresh local `dot_docs_verify` Postgres database (the 0.5.0 entry above had to fall back to `SESSION_DRIVER=array` because this database didn't exist in that session's environment — it does now); `php artisan serve` confirms `/login`, `/register`, `/forgot-password`, and `/reset-password/{token}` render correctly at 1600px and 375px, no overflow, no console errors. `confirm-password` and `two-factor-challenge` are gated behind an authenticated session and were verified by code review only, reusing the same already-verified components. | Redesigned `resources/views/welcome.blade.php` — the ecosystem's guest-facing-design pattern from the Dot.Mines pilot (`d191d10` + its `dfc4547` logo-legibility follow-up) applied to this platform, not copied wholesale. The prior page was a generic dark-SaaS template (near-black `#080f1e` background, bright gold/sky radial-gradient glows, glassmorphism cards, a fake browser-chrome "editor" mockup with invented collaborator initials `A`/`B`/`C` and a scripted "✨ AI suggestion," fabricated stats `∞`/`100%`, an `AI-Powered · Real-time · Offline-Ready` badge) — the same AI-slop pattern the pilot's own changelog (0.3.3) describes. Sampled the real logo (`public/images/logo.png`) with pixel-level color extraction rather than eyeballing it: mustard-gold `#f1c62e` (the open-folder icon) and sky-blue `#2487d4` (the chevron/arrow). Unlike Dot.Mines' single-accent gold system, built a deliberate *dual*-accent system because the Dot.docs mark is explicitly two-toned: gold drives primary actions (CTA buttons), `--blue-deep #1a6bae` (a darkened, AA-safe derivative of the logo blue) drives structural/informational accents (mono tags, section eyebrows) — every text/background pairing was checked against WCAG contrast ratios before use (documented in-session; e.g. ink-on-paper 16.0:1, ink-on-gold 10.5:1, blue-deep-on-paper 5.2:1). The body theme is a warm **paper** palette (`--paper #fbf7ee`, `--ink #1f1b14`) rather than Dot.Mines' all-dark ink theme — deliberately different, not a variation for its own sake: the real logo sits on white with no dark-mode signal in it, and a paper/ink register suits a document-writing product on its own terms. The hero and CTA sections keep a dark photographic band (each fading toward the paper body), functioning as a "cover" bracketing the lighter "pages" beneath — a structural idea grounded in the product being a document platform, not an aesthetic default. Type: Fraunces (display serif) + Work Sans (body) + IBM Plex Mono (labels) — avoids both the pilot's own pairing (Outfit/Plus Jakarta Sans/JetBrains Mono) and the prior page's Figtree; the serif display leans into an editorial/writing register that a mining fleet-ops tool has no reason to reach for. Layout: asymmetric hero (`flex items-end`, copy anchored top-left over a real photo) and a divided asymmetric feature list (6 items, hairline borders, mono field-tags) replacing the prior page's 3-per-row icon-in-colored-box grid, plus a second divided two-column "capabilities" section, mirroring the pilot's structural pattern. Signature element: a large, quiet line-art **open-folder silhouette** in the hero — envelope outline, an open front-cover "V," two interior crease lines, one grommet — an original stylized glyph echoing the real logo's own folder icon (not a reuse of Dot.Mines' headframe, and not a literal pixel trace of the PNG). Copy rewritten against this wiki's own §1/§3: states only real, implemented capabilities (Reverb-broadcast presence/live cursors, `DocumentVersion` snapshots with side-by-side diffs, threaded/resolvable comments with @mentions, the four real sharing modes — personal / team / named-collaborator-with-role / public-with-password-and-expiry — the actual AI-assist list from §1, and the real PDF/Word/HTML/Markdown export formats); removed the fabricated stats, the fake mockup, and the "Free"/trial CTA language, since nothing in this wiki documents a paid tier or trial period for this platform (CTAs now read "Start writing" / "Sign in"). Removed the prior page's dead `#how-it-works` and `#templates` anchors, which pointed at an offline-mode/template-gallery framing not corroborated by §1 in that form. Motion: one restrained `IntersectionObserver` scroll-reveal (`prefers-reduced-motion` respected) plus `scale(0.97)` press feedback — no `animate-pulse` status dots or ambient glow-blob motion, both present on the prior page. <br><br>**Real technical finding not present in the pilot:** this app's `resources/js/app.js` does not import Alpine directly (unlike Dot.Mines' app.js) — it only registers `Alpine.data(...)` inside an `alpine:init` listener and relies entirely on Livewire's bundled Alpine instance. The guest welcome page never loads `@livewireScripts`, so `window.Alpine` would never exist there; copying the pilot's `x-data`/`@click`/`x-show` nav markup verbatim would have shipped dead attributes (mobile menu never opens, scroll-state nav background never applies) without failing any build step. Verified by reading `resources/js/app.js` and `bootstrap.js` side by side before writing markup, not assumed. Implemented the nav's scroll-state background and mobile-menu toggle as a small self-contained inline `<script>` in `welcome.blade.php` using plain DOM APIs instead, rather than touching the shared `app.js` (which would risk a double-Alpine-instance conflict on authenticated Livewire pages that do load Livewire's own copy). **Also found and fixed a bug in my own first draft during verification** (never reached the committed version): Tailwind's `text-[var(--x)]/NN` opacity-modifier syntax does not resolve against a nested CSS custom property — it silently falls back to the inherited text color instead of erroring, which rendered the hero subheadline, the capability strip, and the CTA body copy in near-invisible dark ink over the dark photo bands. Fixed by adding precomputed rgba tokens (`--cream-80`, `--cream-75`, `--cream-40`, `--ink-60`) to `:root` and swapping every opacity-modified arbitrary-var utility to reference them directly; caught via `getComputedStyle(...).color` inspection, not by eyeballing a screenshot. Verified end-to-end: `npm install` + `npm run build` clean (this repo's Tailwind v3 pipeline via `postcss.config.js`/`tailwind.config.js`; the `@tailwindcss/vite` v4 devDependency in `package.json` is unused/unwired, pre-existing, out of scope here). Rendered via a local `php artisan serve` preview (PHP 8.5.9 via Homebrew was available this pass) with `SESSION_DRIVER=array`/`CACHE_STORE=array` set on the server process only, since the shared `infodot`/`dot_docs_verify` Postgres database was not provisioned in this environment — the committed `.env` was not touched. Confirmed in-browser at 375px, 500px, 1600px, and 1920px viewports: nav logo renders at 56px (mobile) / 64px (desktop) and footer logo at 44px, measured via `getBoundingClientRect()` rather than guessed — directly applying the lesson from the pilot's `dfc4547` follow-up fix. No horizontal overflow at any width (`scrollWidth === innerWidth`, checked programmatically). Mobile hamburger menu opens and closes correctly; zero console errors. **Build artifacts:** unlike Dot.Mines, this repo's `.gitignore` excludes `/public/build` and always has (checked full git history — never once tracked), so the rebuilt Vite assets are intentionally not committed here; only `welcome.blade.php` and this wiki entry are staged, matching this repo's own established convention rather than the pilot's. |
| 0.4.0 | 2026-08-04 | Platform-loop pass | **Evaluated Dot.docs for the ecosystem-wide `HasUserScope`/`HasTeamScope` global-scope pattern (Dot.Finance 2f75bdb, Dot.Notify e671436) and deliberately did NOT apply it.** Every candidate model was checked against the real schema, not assumed: `documents.team_id`, `document_templates.team_id`, and `document_slash_commands.team_id` are all **nullable** foreign keys (see their migrations), and §2/§6 of this wiki already documents that Dot.docs is explicitly *not* exclusively team-scoped — a document can be personal (`owner_id` only, `team_id` null), team-attached, shared to named collaborators (`document_collaborators`), or public, with `DocumentPolicy` as the single source of truth (confirmed correct in the 0.1.0 pass). `DocumentTemplate` adds a fourth path (`is_global`). A blind `where('team_id', currentTeam)` global scope on any of these would have been a regression, not hardening: every personal document would vanish from its own owner's queries (`team_id IS NULL` never equals `currentTeam->id`), every `is_global` template would vanish for everyone, `TemplateGallery`'s existing global/team/own-authorship OR-logic (the IDOR fix from the 0.1.0 pass) would break, and `Editor::acceptSuggestion()`/`rejectSuggestion()` — which deliberately let any authorized collaborator resolve *any other collaborator's* pending `AiSuggestion` on a document — would 404 on cross-user suggestions. Same reasoning ruled out `Comment`, `DocumentVersion`, `DocumentCollaborator`, and `DocumentWebhook`: each is a document-child record whose visibility is correctly gated by `DocumentPolicy` + an explicit `where('document_id', ...)` already, not by the *record's own* `user_id`/`created_by` (confirmed by reading `WebhookManager`, `CommentThread`, `VersionHistory`, `Editor` directly — none of them filter these models by their own owner column for listing). Added `tests/Feature/DocumentAccessScopeTest.php` in place of the scaffolded "scope alone blocks cross-tenant access" test: it proves the two concrete cases a naive scope would have broken instead — a teamless personal document stays owner-accessible and outsider-blocked via `DocumentPolicy` (not a scope), and a private team template is invisible to an outside team while an `is_global` template is not. This is the same "use your own judgment, some Jetstream-teams apps stay user-scoped" carve-out the rollout brief called out, taken one step further: here neither single-column pattern fits, because access is genuinely multi-path. <br><br>While adding PHPStan/Larastan config, `vendor/bin/phpstan analyse --memory-limit=1G` actually ran (no sandbox silent-failure this time) and immediately hit a real parse error: `app/Notifications/CommentPostedNotification.php` and `app/Notifications/MentionedInCommentNotification.php` were both missing their final two closing braces (`}` for the last method, `}` for the class) — genuine pre-existing fatal syntax errors that would crash any code path notifying about a comment/mention (`php -l` confirmed both were broken before this pass, and neither has test coverage that exercises them, which is why CI/`php artisan test` never caught it). Fixed both with the two-line brace close; `php -l` now passes on every file under `app/`. With that fixed, PHPStan level 5 ran to completion and reported 39 pre-existing errors (mostly level-5-typical Eloquent dynamic-property/PHPDoc-covariance noise on `Model::$id`/`$name`, `Team`/`User` `$fillable` covariance, one real-looking `Comment::extractMentions()` null-coalesce-on-non-nullable-offset finding) — none introduced by this pass, left unfixed as a separate follow-up (config + baseline is the deliverable here, not a level-5 cleanup). `composer audit` found 30 advisories across 10 packages (`guzzlehttp/guzzle` 9, `guzzlehttp/psr7` 4, `dompdf/dompdf` 6, `laravel/framework` 3, and five `symfony/*` packages); fixed the prescribed guzzle family via `composer update guzzlehttp/guzzle guzzlehttp/psr7 guzzlehttp/promises --with-all-dependencies` (7.10.0→7.15.2 / 2.9.0→2.13.0 / 2.3.0→2.5.1), bringing it to 17 advisories across 8 packages — guzzle fully clean; the remaining dompdf/laravel/symfony advisories are out of this pass's prescribed scope (would need major-version bumps, not a dependency-family patch) and are flagged here for a dedicated pass rather than silently left undocumented. <br><br>Verified for real against a fresh, isolated PostgreSQL database (`dot_docs_pilot`, dropped after): `php artisan migrate` ran clean (confirms the 0.3.0 pass's migration-ordering fix still holds), full suite before any change: 44 tests / 40 passed / 4 skipped / 0 failed; after the two Notification fixes, the guzzle bump, and the new `DocumentAccessScopeTest`: 47 tests / 43 passed / 4 skipped / 0 failed. No 403→404 assertion changes were needed or made — no scope was added, so route-model-binding behavior is unchanged. |
| 0.3.1 | 2026-08-03 | Sakhile Bhayi | Fixed the README's logo: it referenced `docs/logo.svg`, an auto-generated generic placeholder badge (dark card + platform name text), not this platform's real designed logo. Swapped to the real PNG logo already used elsewhere in this repo, and removed the now-dead `docs/logo.svg` placeholder file entirely rather than leaving it alongside the real asset. |
| 0.3.0 | 2026-08-03 | Sakhile Bhayi | Real-execution verification pass — this platform was actually installed and run against real PHP/PostgreSQL for the first time (composer install ran clean under the default PHP 8.5, no downgrade needed). `php artisan migrate` failed on the first real run with a genuine ordering bug: `2026_04_10_093152_create_document_collaborators_table.php` and `2026_04_10_093152_create_documents_table.php` shared an identical timestamp, and alphabetical tie-breaking ran `document_collaborators` (which foreign-keys to `documents`) before `documents` itself was created. Fixed by retimestamping `create_document_collaborators_table` to `093154` so it runs after `documents` (093152) and its siblings `ai_suggestions`/`comments`/`document_versions` (093153), all of which only depend on `documents`/`users`. After the fix, `php artisan migrate` ran clean and `php artisan test` passed 40/44 (4 skipped — features gated off by config, not real failures). Applied the Dot.Brain adr/ADR-0013 idempotent guard to this platform's six shared Jetstream-core migrations (users/password_reset_tokens/sessions, two-factor columns, personal_access_tokens, teams, team_user, team_invitations), each now wrapped in `Schema::hasTable`/`hasColumn` checks so this platform's copies are safe to co-execute with any other Dot platform against the same shared `infodot` database. Re-verified against a fresh, empty database after guarding: migrate clean, same 40 passed / 4 skipped / 0 failed test result, confirming the guard changes no observable behavior. |
| 0.2.0 | 2026-08-03 | Sakhile Bhayi | Redesigned `resources/views/welcome.blade.php`'s hero background: the nav/footer `dot_doc.png` logo was already real from a prior pass, but the hero section (`.hero-bg`) used only abstract radial-gradient glows with no photographic content. Added a real, licensed Unsplash photo of a team collaborating in an office by Vitaly Gariev (@silverkblack), unsplash.com/photos/l6I8PWGnAVQ, hotlinked via Unsplash's CDN with photographer credit as an inline HTML comment, layered under a dark linear-gradient overlay so the existing gold/sky gradient text and dot-grid overlay keep WCAG-adequate contrast. Verified the CDN URL resolves (`curl -sI` returned `HTTP/2 200`) before committing. |
| 0.1.0 | 2026-08-02 | Docs Platform Lead | Initial platform-owned wiki, derived from the actual Laravel codebase. Verified the `EcosystemAuthController` SSO contract (correct) and fixed a real `DB_DATABASE` misconfiguration in `.env.example` (was falling back to a nonexistent `laravel` database instead of the shared `infodot` instance). Fixed two IDOR gaps found in this pass's security scan: unscoped `DocumentVersion::find()` in `VersionHistory` and unscoped `DocumentTemplate::findOrFail()` in `TemplateGallery`. Wired the existing `dot_doc.png` logo into the app-shell favicon (was only wired into `welcome`/`guest` layouts) and generated the standard ecosystem favicon set. Corrected README.md's stack claims (Laravel 12→13, "Anthropic Claude"→OpenAI/gpt-4o, removed unimplemented Redis/Horizon/Scout/Meilisearch claims, corrected the domain-model list). |

## Open Questions

- Should `task-list.md` be deleted/archived now that this wiki is the accurate source, or kept as historical provenance per Manifesto principle 6? Leaning toward keeping it, unedited, as a record of original intent vs. actual build — same reasoning Dot.Billing's wiki applies to its own gaps.
- No Stripe-equivalent sensitive-data concern exists here, but public document sharing (password + expiry) has not been security-reviewed beyond the IDOR scan in §6 — a dedicated pass on the public `/shared/{uuid}` routes (timing attacks on password check, brute-forceability) is worth scheduling.
